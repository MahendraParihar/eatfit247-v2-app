<%
  /*
   * Opens a meta-card section inside the body: uppercase eyebrow label + 2-col rows.
   * Pair with partials/section-close.ejs. Rows are emitted with partials/info-row.ejs.
   *
   * Locals:
   *   eyebrow (string, required) - uppercase section label (e.g. 'Invoice details', 'Charges')
   */
-%>
<tr>
  <td class="px" style="padding: 8px 40px 8px;">
    <table role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" style="background-color:#f3f4f6; border-radius:12px; border:1px solid #e5e7eb;" class="dark-card dark-border">
      <tr><td style="padding:18px 20px 6px; font-family:Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif; font-size:12px; font-weight:700; letter-spacing:0.12em; text-transform:uppercase; color:#9ca3af;" class="dark-muted"><%= eyebrow %></td></tr>
      <tr>
        <td style="padding:4px 20px 18px;">
          <table role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" class="meta-grid">
